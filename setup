#!/usr/bin/env bash

pacman -Sy git --needed --noconfirm
git clone https://github.com/c0d3d3v/aui
cd aui || exit

git checkout automation
echo "Starting liveinstall. Logging to $PWD/aui.log"
bash -c "./liveinstall | tee -a aui.log"
